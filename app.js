function showRatings (rating){
    let ratings = ""
    for (let i = 0; i < Math.floor(ratings); ++i){
        ratings += "*"
        if (i !== ratings.isInteger(ratings)){

        }
if(!Number.isInteger(rating) )
ratings += " ."

    }
    return ratings;
}

console.log(str([4,2]))